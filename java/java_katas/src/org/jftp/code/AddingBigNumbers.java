package org.jftp.code;

public class AddingBigNumbers {
	public String add(String a, String b) {
		int banking = 0;

		char[] longItems;
		char[] shortItems;

		if(a.trim().length() > b.trim().length()) {
			longItems = a.toCharArray();
			shortItems = b.toCharArray();
		} else {
			longItems = b.toCharArray();
			shortItems = a.toCharArray();
		}

		System.out.println(longItems);

		System.out.println(shortItems);

		for (int l = 0; l)


		String output = "";

		return output;
	}
}
